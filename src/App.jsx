import React, { Component } from 'react';

var App = React.createClass({
  render() {
    return (
      <div className="App">
        <button>Login with google</button>
      </div>
    );
  }
});

export default App;
